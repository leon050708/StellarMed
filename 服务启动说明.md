# StellarMed 服务启动说明

## ✅ 当前服务状态

所有服务已成功启动并注册到 Nacos！

| 服务名称 | 端口 | 状态 | 访问地址 |
|---------|------|------|----------|
| patient-service | 8101 | ✅ 运行中 | http://localhost:8101 |
| symptom-ai-service | 8201 | ✅ 运行中 | http://localhost:8201 |
| diagnosis-ai-service | 8202 | ✅ 运行中 | http://localhost:8202 |
| test-suggestion-ai-service | 8083 | ✅ 运行中 | http://localhost:8083 |
| summary-ai-service | 8204 | ✅ 运行中 | http://localhost:8204 |
| prescription-ai-service | 8085 | ✅ 运行中 | http://localhost:8085 |
| doctor-confirm-service | 8301 | ✅ 运行中 | http://localhost:8301 |

## 🚀 服务管理命令

### 使用管理脚本

```bash
# 查看服务状态
./manage-services.sh status

# 启动所有服务
./manage-services.sh start

# 停止所有服务
./manage-services.sh stop

# 重启所有服务
./manage-services.sh restart
```

### 使用启动脚本

```bash
# 一键启动所有服务
./start-all-services.sh
```

## 📋 服务功能说明

### 1. patient-service (8101)
- **功能**: 患者服务，核心编排服务
- **主要接口**:
  - `/api/patients/**` - 患者管理
  - `/api/sessions/**` - 会话管理
  - `/api/chat/**` - AI对话
  - `/api/assist/**` - AI助诊流程

### 2. symptom-ai-service (8201)
- **功能**: 症状结构化AI服务
- **主要接口**:
  - `POST /api/ai/symptoms/extract` - 提取结构化症状

### 3. diagnosis-ai-service (8202)
- **功能**: 诊断AI服务（初步诊断和风险评估）
- **主要接口**:
  - `POST /api/ai/diagnosis/evaluate` - AI初步诊断
  - `POST /api/ai/risk/evaluate` - AI风险评估

### 4. test-suggestion-ai-service (8083)
- **功能**: 检查建议AI服务
- **主要接口**:
  - `POST /api/ai/test-suggestions/suggest` - 生成检查建议

### 5. summary-ai-service (8204)
- **功能**: 会话总结AI服务
- **主要接口**:
  - `POST /api/ai/session-summary/generate` - 生成会话总结

### 6. prescription-ai-service (8085)
- **功能**: 处方建议AI服务
- **主要接口**:
  - `POST /api/prescription/generate` - 生成处方建议
  - `GET /api/prescription/query` - 查询处方建议

### 7. doctor-confirm-service (8301)
- **功能**: 医生确认服务
- **主要接口**:
  - `GET /api/doctor-confirm/ai-report` - 获取AI助诊报告
  - `POST /api/doctor-confirm/final-diagnosis` - 保存最终诊断

## 📝 日志查看

所有服务的日志文件位于 `logs/` 目录：

```bash
# 查看所有日志
tail -f logs/*.log

# 查看特定服务日志
tail -f logs/patient-service.log
tail -f logs/symptom-ai-service.log
# ... 等等
```

## 🔍 服务健康检查

### 检查 Nacos 注册情况

访问 Nacos 控制台：http://localhost:8848/nacos
- 用户名：nacos
- 密码：nacos

在"服务管理" -> "服务列表"中可以看到所有已注册的服务。

### 检查服务端口

```bash
# 检查所有服务端口
for port in 8101 8201 8202 8083 8204 8085 8301; do
  if lsof -Pi :$port -sTCP:LISTEN -t >/dev/null 2>&1; then
    echo "✓ 端口 $port 已启动"
  else
    echo "✗ 端口 $port 未启动"
  fi
done
```

## ⚠️ 注意事项

1. **Nacos 服务器**: 确保 Nacos 服务器已启动（localhost:8848）
2. **数据库连接**: 确保数据库可访问（82.157.188.65:2000）
3. **JDK 版本**: 所有服务使用 JDK 17
4. **端口占用**: 如果端口被占用，脚本会自动跳过该服务

## 🛑 停止所有服务

```bash
# 方法1: 使用管理脚本
./manage-services.sh stop

# 方法2: 直接杀死所有 Spring Boot 进程
pkill -f 'spring-boot:run'
```

## 📞 故障排查

如果服务启动失败：

1. **查看日志**: `tail -f logs/<service-name>.log`
2. **检查端口**: 确认端口未被其他程序占用
3. **检查数据库**: 确认数据库连接正常
4. **检查 Nacos**: 确认 Nacos 服务器运行正常
5. **检查 JDK**: 确认使用 JDK 17

## 🔄 服务启动顺序建议

虽然脚本会并行启动所有服务，但建议的依赖顺序是：

1. **基础服务**:
   - symptom-ai-service
   - diagnosis-ai-service
   - test-suggestion-ai-service
   - summary-ai-service
   - prescription-ai-service

2. **编排服务**:
   - patient-service (依赖上述AI服务)

3. **医生端服务**:
   - doctor-confirm-service (依赖 summary-ai-service)

