spring:
  application:
    name: test-suggestion-ai-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848  # Nacos 核心 API 端口（Java 客户端连接）
        file-extension: yaml
        namespace: dev
        group: DEFAULT_GROUP
        # 鉴权已关闭，不需要配置用户名密码

