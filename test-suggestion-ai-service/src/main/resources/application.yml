server:
  port: 8083

spring:
  application:
    name: test-suggestion-ai-service
  
  # 数据库配置 (保留你原来的)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://82.157.188.65:2000/pool2?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: mysql_awJcEH

  # Nacos 配置 (全部移到这里！)
  cloud:
    nacos:
      discovery:
        enabled: true   # ✅ 必须为 true
        server-addr: localhost:8848
        namespace: public # 显式指定 public
        group: DEFAULT_GROUP
        register-enabled: true
      config:
        enabled: false  # 暂时不需要配置中心

  # AI 配置
  ai:
    openai:
      api-key: sk-45be1e34b677429db3a614c91fc07f2b
      base-url: https://dashscope.aliyuncs.com/compatible-mode/
      chat:
        options:
          model: qwen-flash
          temperature: 0.8

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 如果 Redis 设置了密码，请在这里填写
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.assist.common.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.neusoft.neu23: debug
    com.alibaba.nacos: warn # 减少 Nacos 刷屏日志